#The capitalize() method returns a string where the first character is upper case, and the rest is lower case.

#წერტილოვანი ნოტაცია არის სინტაქსი, რომელიც გამოიყენება პროგრამირების ენებში, მათ შორის Python-ში, ობიექტის ატრიბუტებსა და მეთოდებზე წვდომისთვის.

#ფუნქციები და მეთოდები len(), append(), insert() და pop() ხშირად გამოიყენება Python-ში მონაცემთა სტრუქტურების, განსაკუთრებით კი სიების მანიპულირებისთვის.